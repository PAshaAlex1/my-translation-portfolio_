# Что такое владение

> **Источник оригинала:** [The Rust Programming Language – What is Ownership](https://doc.rust-lang.org/book/ch04-00-understanding-ownership.html)  
> **Дата доступа:** 24 ноября 2025  
> **Цель:** Демонстрация навыков технического перевода и адаптации стиля документации  
> **Примечание:** Перевод выполнен исключительно в демонстрационных целях. Все права на оригинал принадлежат авторам проекта.

---

## 🧠 Глоссарий ключевых терминов

| Термин (EN)         | Перевод (RU)           | Примечание |
|---------------------|------------------------|------------|
| Ownership           | Владение              | Механизм управления памятью |
| Borrowing           | Заимствование         | Временный доступ к данным |
| Compiler            | Компилятор            | Программа для преобразования кода |
| Module              | Модуль                | Структурная единица кода |
| Function            | Функция               | Блок кода, выполняющий задачу |
| Stack               | Стек                  | Область памяти для локальных данных |
| Heap                | Динамическая память   | Область памяти для динамических данных |

---

## Что такое владение?

*Владение* — это набор правил, определяющих, как программа на языке Rust управляет памятью. Все программы так или иначе должны управлять тем, как они используют память компьютера во время работы. Некоторые языки имеют сборщик мусора, регулярно отслеживающий неиспользуемую память во время работы программы; в других языках программист должен явно выделять и освобождать память. В Rust используется третий подход: память управляется через систему владения с набором правил, которые проверяются компилятором. При нарушении любого из правил программа не будет скомпилирована. Ни одна из особенностей системы владения не замедлит работу вашей программы.

Поскольку система владения является новой концепцией для многих программистов, потребуется некоторое время, чтобы привыкнуть к ней. Хорошая новость заключается в том, что по мере накопления опыта работы с Rust и его правилами разработчику становится проще создавать безопасный и производительный код.. Продолжайте практиковаться!

Поняв, как работает владение, вы получите базу для понимания ключевых возможностей Rust. . В этой главе вы познакомитесь с системой владения на примере одной из самых распространённых структур данных — строках.

> ### Стек и динамическая память
>
> Во многих языках программирования вам редко приходится задумываться о стеке и динамической памяти. Но в системном языке, таком как Rust, расположение значения — в стеке или в области динамического распределения памяти — влияет на поведение языка и объясняет, почему необходимо принимать определённые решения. В дальнейшем в этой главе концепция владения будет рассмотрена в контексте стека и динамической памяти; ниже приводится краткое пояснение.
>
> И стек, и динамическая память — это области памяти, доступные вашему коду во время выполнения, но они организованы по-разному. Стек хранит значения в порядке их поступления и удаляет их в обратном порядке. Такой принцип называется *«последним пришёл — первым ушёл»* (last in, first out). Представьте стопку тарелок: новые тарелки кладутся сверху, а когда нужна тарелка, её берут тоже сверху. Добавление или удаление тарелок из середины или снизу было бы неудобным! Добавление данных в стек называется *«поместить»* (push), а удаление — *«извлечь»* (pop). Все данные, хранящиеся в стеке, должны иметь известный и фиксированный размер. Данные с неизвестным размером во время компиляции или с размером, который может изменяться, должны храниться в динамической памяти.
>
> Динамическая память менее организована: когда вы помещаете туда данные, вы запрашиваете определённый объём пространства. Система выделения памяти находит свободный участок достаточного размера, помечает его как используемый и возвращает *указатель* — адрес этой области. Этот процесс называется *выделением памяти в динамической области* (часто сокращённо — *«выделение»*). Добавление значений в стек не считается выделением.
> Так как указатель на динамическую память имеет известный и фиксированный размер, его можно хранить в стеке, но для доступа к самим данным необходимо следовать по указателю. Представьте ситуацию в ресторане: вы входите и сообщаете количество человек, а хостес находит свободный стол подходящего размера и проводит вас. Если кто-то из вашей компании опоздает, он сможет спросить, где вы сидите, чтобы найти вас.
>
> Помещение данных в стек выполняется быстрее, чем выделение памяти в динамической области, потому что системене нужно искать место для новых данных: оно всегда находится на вершине стека. Для сравнения, выделение пространства в динамической памяти требует больше работы, так как система должна сначала найти участок достаточного размера, а затем выполнить дополнительные операции учёта для подготовки к следующему выделению.
>
> Доступ к данным в динамической памяти обычно медленнее, чем доступ к данным в стеке, потому что необходимо пройти по указателю, чтобы их получить. Современные процессоры работают быстрее, если им приходится меньше «прыгать» по разным участкам памяти. Продолжая аналогию: официант в ресторане принимает заказы от нескольких столов. Наиболее эффективно собрать все заказы за одним столом, прежде чем переходить к следующему. Если же брать заказ то у стола A, то у стола B, потом снова у A и снова у B, процесс будет значительно медленнее. По той же причине процессор обычно лучше справляется с задачами, когда работает с данными, расположенными рядом друг с другом (как в стеке), а не с данными, находящимися далеко друг от друга (как в динамической памяти).
>
> В момент вызова функции в стек помещаются все переданные ей значения (включая, при необходимости, указатели на данные, размещенные в динамической памяти), а также локальные и переменные функции. По завершении работы функции данные извлекаются из стека.
>
> Контрольза использованием данных в динамической памяти различными частями программы, минимизируя количество дублирующихся данных и очистка неиспользуемых данных для предотвращения исчерпания ресурсов - это задачи, которые решает система владения. После освоения концепции владения необходимость постоянного анализа стека и динамической памяти отпадает, однако понимание того, что основная функция владения заключается в управлении данными динамической памяти, позволяет глубже осознать принципы его работы.

## Правила владения

Прежде всего обратимся к базовым правилам владения, которые необходимо учитывать при изучении примеров:
- Каждое значение в Rust связано с конкретным *владельцем*.
- В каждый момент времени допускается существование лишь одного владельца.
- При выходе владельца из области видимости соответствующее значение удаляется.

## Облость видимости переменной

Теперь, когда мы разобрали базовый синтаксис Rust, в примерах мы больше не будем включать весь код с `fn main() {`.  При самостоятельном выполнении примеров необходимо вручную помещать их в функцию `main`. Это позволит сделать примеры более лаконичными и сосредоточить внимание на содержательных аспектах, а не на шаблонном коде.

В качестве первого примера владения мы рассмотрим *область видимости* некоторых переменных. Область видимости — это диапазон внутри программы, в котором элемент остаётся допустимым. Рассмотрим следующую переменную:

```rust
let s = "hello";
```
Переменная `s` представляет строковый литерал, значение которого жёстко задано в исходном коде программы. Данная переменная считается действительной начиная с момента объявления и до завершения текущей области видимости. В листинге 4-1 приведён пример программы с комментариями, указывающими границы допустимости переменной s.

```rust
{ // здесь переменная s пока не работает, так как она еще не объявлена
    let s = "hello"; // переменная s действительна с этого момента

    // используем переменную s
    println!("{}", s);

} // здесь область видимости переменной s заканчивается, и она становится недействительной
```
<span class="caption">Листинг 4-1: переменная и область действия, в которой она действительна</span>
Другими словами, здесь есть два важных момента:

- Когда переменная `s` *появляется* в области видимости, она считается действительной,
- Она остаётся действительной до момента *выхода* за границы этой области.

На данном этапе соотношение между областями видимости и допустимостью переменных соответствует аналогичным механизмам в других языках программирования. Теперь мы расширим это понимание, познакомившись с типом `String`.

### Тип данных `String`

Для демонстрации принципов владения необходимо использовать тип данных, обладающий большей сложностью по сравнению с теми, что были рассмотрены в разделе «Типы данных» главы 3. . Ранее описанные типы имеют известный размер, могут храниться в стеке и извлекаться из него по завершении области видимости, а также легко и быстро копируются для создания новой независимой копии, если другой части кода нужно использовать то же значение в другой области.  Однако в данном случае нас интересуют данные, размещаемые в динамической памяти, и механизм, посредством которого Rust определяет момент их удаления. Тип `String` служит наглядным примером для такого анализа.

Мы сосредоточимся на тех частях типа `String`, которые связаны с владением. Эти принципы применимы и к другим сложным типам данных — как предоставленным стандартной библиотекой, так и созданным вами. Более подробно тип `String` будет рассмотрен в главе 8.

Мы уже познакомились со строковыми литералами, где значение строки жёстко прописано в программе. Строковые литералы удобны, но они подходят не для всех случаев, когда требуется использовать текст. Во первых — они неизменяемы. Во вторых — не каждое строковое значение может быть известно на этапе написания кода: например, если мы хотим получить ввод от пользователя и сохранить его. Для таких ситуаций в Rust существует второй строковый тип — `String`. Этот тип управляет данными, выделенными в динамической памяти, и способен хранить текст неизвестного размера во время компиляции. Создать `String` из строкового литерала можно с помощью функции `from`, например так:

```rust
let s = String::from("hello");
```

Оператор `::` (двойное двоеточие) используется для помещения функции `from` в пространство имён типа `String`, что исключает необходимость введения отдельного идентификатора, например `string_from`. Данный синтаксис будет подробно рассмотрен в разделе «Синтаксис метода» главы 5, а также в контексте именования и пространств модулей в разделе «Пути для обращения к элементу в дереве модулей» главы 7.

Строка такого типа *может* быть изменяема:

```rust
let mut s = String::from("hello");

    s.push_str(", world!"); // push_str() присоединяет строковый литерал к существующей строке типа String

    println!("{s}"); // программа выведет строку `hello, world!`

```

Так в чём же разница? Почему String можно изменять, а строковые литералы — нет? Разница заключается в том, как эти два типа работают с памятью.

### Память и способы её выделения

В случае строкового литерала его содержимое определяется на этапе компиляции, вследствие чего текст непосредственно включается в итоговый исполняемый модуль. Благодаря этому строковые литералы отличаются высокой скоростью и эффективностью, что обусловлено их неизменяемостью. Однако невозможно встроить в бинарный файл блок памяти для каждой строки, размер которой не может быть определён во время компиляции и способен изменяться в процессе выполнения программы.

В случае типа `String`, чтобы поддерживать изменяемый и расширяемый текст, необходимо выделить участок памяти в динамической памяти, размер которого неизвестен во время компиляции, для хранения содержимого. Это означает:
- Память должна быть запрошена у распределителя памяти во время выполнения.
- Должен существовать способ вернуть эту память распределителю, когда мы закончим использовать наш `String`.
Первая часть выполняется нами: при вызове `String::from` его реализация запрашивает необходимую память. Это практически универсальный механизм в языках программирования.

Однако вторая часть отличается. В языках с автоматическим *сборщиком мусора* (GC) он отслеживает и очищает память, которая больше не используется, и нам не нужно об этом думать. В большинстве языков без GC ответственность ложится на программиста: нужно определить момент, когда память больше не используется, и явно вызвать код для её освобождения — так же, как мы её запрашивали. Делать это правильно исторически было сложной задачей. Если забыть — память будет потрачена впустую. Если освободить слишком рано — переменная станет недействительной. Если освободить дважды — это тоже ошибка. Нужно, чтобы каждая операция `выделения` соответствовала ровно одной операции `освобождения`.

Rust реализует иной подход: память автоматически освобождается, как только переменная, которой она принадлежит, выходит из области видимости. Ниже приведена версия нашего примера из листинга 4-1, где вместо строкового литерала используется тип `String`.

```rust
{
    let s = String::from("hello"); // переменная s действительна с этого момента

        // используем переменную s
    }                                // здесь область видимости переменной s заканчивается, и она становится недействительной
```

Возврат памяти, необходимой объекту `String`, осуществляется в момент выхода переменной `s` из области видимости. В этот момент Rust инициирует вызов специальной функции, именуемой `drop`. В данной функции разработчик типа `String` реализует процедуру освобождения памяти. Вызов `drop` производится автоматически компилятором Rust при достижении закрывающей фигурной скобки.
>
> Примечание: в языке C++ данный способ, освобождения ресурсов по завершении времени жизни объекта именуется *«Resource Acquisition Is Initialization» 'Получение ресурса есть инициализация' (RAII)*. Функция `drop` в Rust будет понятна тем, кто имеет опыт применения паттерна RAII.

Этот подход оказывает серьёзное влияние на то, как пишется код на Rust. Сейчас он может показаться простым, но поведение программы может быть неожиданным в более сложных ситуациях, когда мы хотим, чтобы несколько переменных использовали данные, выделенные в динамической памяти. Давайте рассмотрим некоторые из этих случаев.

#### Взаимодействие переменных и данных с помощью перемещения

Несколько переменных могут взаимодействовать с одними и теми же данными по‑разному в Rust. Давайте рассмотрим пример с использованием целого числа в листинге 4‑2.

```rust
let x = 5;
let y = x;
```
Листинг 4-2. Присвоение целочисленного значения переменной `x` переменной `y`

Очевидно, что данный фрагмент кода выполняет следующее: «значение 5 связывается с переменной `x`; затем создаётся копия значения `x`, которая связывается с переменной `y`». В результате мы имеем две переменные, `x` и `y`, каждая из которых содержит значение 5. Такое поведение объясняется тем, что целые числа представляют собой простые значения фиксированного размера, и оба экземпляра числа 5 размещаются в стеке.

Перейдём к рассмотрению примера, в котором используется тип `String`.

```rust
let s1 = String::from("hello");
let s2 = s1;
```

Это выглядит очень похоже, поэтому можно предположить, что работает оно так же: то есть вторая строка создаёт копию значения из `s1` и связывает её с `s2`. Но на самом деле всё происходит немного иначе.

Посмотрите на рисунок 4‑1, чтобы понять, что происходит со строкой `String` «под капотом». Она состоит из трёх частей, показанных слева: указателя на область памяти (ptr), где хранится содержимое строки, длины (len) и ёмкости (capacity). Эта группа данных хранится в стеке. Справа показана динамическая память, где находится само содержимое.

<img alt="Two tables: the first table contains the representation of s1 on the&lt;br&gt;stack, consisting of its length (5), capacity (5), and a pointer to the first&lt;br&gt;value in the second table. The second table contains the representation of the&lt;br&gt;string data on the heap, byte by byte." src="https://github.com/rust-lang-ru/book/blob/master/rustbook-ru/src/img/trpl04-01.svg?raw=true" class="">

<span class="caption">Рисунок 4-1: представление в памяти <code>String</code>, содержащей значение <code>"hello"</code>, привязанное к <code>s1</code></span>

Длина — это объём памяти в байтах, который в настоящее время использует содержимое `String`. Ёмкость — это общий объём памяти в байтах, который `String` получил от распределителя. Разница между длиной и ёмкостью имеет значение, но не в этом контексте, поэтому на данный момент можно игнорировать ёмкость.

Когда мы присваиваем `s1` значению `s2`, данные `String` копируются, то есть мы копируем указатель, длину и ёмкость, которые находятся в стеке. Мы не копируем данные в динамической памяти, на которые указывает указатель. Другими словами, представление данных в памяти выглядит так, как показано на рис. 4-2.

<img alt="Three tables: tables s1 and s2 representing those strings on the&lt;br&gt;stack, respectively, and both pointing to the same string data on the heap." src="https://github.com/rust-lang-ru/book/blob/master/rustbook-ru/src/img/trpl04-02.svg?raw=true" class="">

<span class="caption">Рисунок 4-2: представление в памяти переменной <code>s2</code>, имеющей копию указателя, длины и ёмкости <code>s1</code></span>

Представление *не* похоже на рисунок 4-3, как выглядела бы память, если бы вместо этого Rust также скопировал данные из динамической памяти. Если бы Rust сделал это, операция `s2 = s1` могла бы быть очень дорогой с точки зрения производительности во время выполнения, если бы данные в динамической памяти были большими.

<img alt="Two tables: the first table contains the representation of s1 on the&lt;br&gt;stack, consisting of its length (5), capacity (5), and a pointer to the first&lt;br&gt;value in the second table. The second table contains the representation of the&lt;br&gt;string data on the heap, byte by byte." src="https://github.com/rust-lang-ru/book/blob/master/rustbook-ru/src/img/trpl04-01.svg?raw=true" class="">

<span class="caption">Рисунок 4-3: другой вариант того, что может сделать <code>s2 = s1</code>, если Rust также скопирует данные из динамической памяти</span>

Ранее мы говорили, что когда переменная выходит из области видимости, Rust автоматически вызывает функцию `drop` и очищает динамическую память, связанную с этой переменной. Но на рисунке 4‑2 видно, что оба указателя данных указывают на одно и то же место. Это проблема: когда `s2` и `s1` выйдут из области видимости, они оба попытаются освободить одну и ту же память. Такая ситуация называется *ошибкой двойного освобождения (double free error)* и относится к багам, связанным с безопасностью работы с памятью. Двойное освобождение может привести к повреждению памяти и потенциально к уязвимостям в системе безопасности.

Чтобы обеспечить безопасность работы с памятью, после строки `let s2 = s1;` Rust считает переменную `s1` недействительной. Поэтому при выходе `s1` из области видимости освобождать память не требуется. Попробуйте использовать `s1` после создания `s2` — это не сработает.

```Rust
    let s1 = String::from("hello");
    let s2 = s1;

    println!("{s1}, world!");
```

Rust выдаст ошибку вроде этой — он не позволит вам обратиться к переменной, которая уже считается недействительной:

```Rust
$ cargo run
   Компиляция проекта ownership версии 0.1.0 (file:///projects/ownership)
ошибка[E0382]: попытка заимствования перемещённого значения: s1
 --> src/main.rs:5:15
  |
2 |     let s1 = String::from("hello");
  |         -- Причина перемещения заключается в том, что переменная s1 имеет тип String, для которого не реализован трейт Copy.
3 |     let s2 = s1;
  |              -- Значение перемещено в данной точке
4 |
5 |     println!("{s1}, world!");
  |               ^^^^ Вы пытаетесь взять ссылку на значение в этом месте, хотя оно уже было перемещено
  |
  = Примечание: эта ошибка возникает в макросе $crate::format_args_nl, который появился в результате развёртывания макроса println (в Nightly‑сборках можно запустить с ключом -Z macro-backtrace для получения дополнительной информации).
Подсказка: рассмотрите возможность клонирования значения, если допустимы затраты на производительность.

  |
3 |     let s2 = s1.clone();
  |                ++++++++

Для получения дополнительной информации об этой ошибке попробуйте выполнить rustc --explain E0382.
ошибка: не удалось скомпилировать проект ownership (bin "ownership") из‑за одной предыдущей ошибки.
```

Если вы сталкивались с терминами *«поверхностное копирование» (shallow copy)* и *«глубокое копирование» (deep copy)* в других языках, то идея копирования указателя, длины и ёмкости без копирования самих данных может показаться поверхностным копированием. Но поскольку Rust также делает первую переменную недействительной, это называется не поверхностным копированием, а *перемещением*. В данном примере мы бы сказали, что `s1` был перемещён в `s2`. То, что происходит на самом деле, показано на рисунке 4‑4.

<img alt="Три таблицы: таблицы s1 и s2, представляющие эти строки в стеке соответственно, и обе указывающие на одни и те же строковые данные в куче. Таблица s1 выделена серым цветом, потому что s1 больше недействительна; только s2 можно использовать для доступа к данным кучи." src="img/ferris/trpl04-04.svg" class="center" style="width: 50%;">

<span class="caption">Рисунок 4-4: представление в памяти после того, как <code>s1</code> был признан недействительным</span>

Проблема решена! Так как действительной остаётся только переменная `s2`, при выходе из области видимости именно она освободит память — и на этом всё.
Кроме того, здесь подразумевается важное решение в дизайне языка: Rust никогда не создаёт «глубокие» копии ваших данных автоматически. Следовательно, любое *автоматическое* копирование в языке можно считать лёгким и быстрым — оно не тратит лишние ресурсы.